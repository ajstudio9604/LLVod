




import SwiftUI


struct HpGKsqoyNddO: View {
    let sources: [ZhopfUeTrMREiEzrLsF]
    let selectedIndex: Int
    @Binding var isPresented: Bool
    var onSelect: (Int) -> Void

    var body: some View {
        
        cUpisToyuBnuzUxVl()
    }

    
    @ViewBuilder
    @inline(never)
    private func cUpisToyuBnuzUxVl() -> some View {
        // source-obfuscator:padding:v1
        let _ = Self.oHQWEpErsmESYqNX()

        pCpfovCmByKHic
    }

    @ViewBuilder
    private var pCpfovCmByKHic: some View {
        ZStack {
            hBTIEqKxDMzbqIA
            qApSadgkUtwnxu
        }
    }


    @ViewBuilder
    private var hBTIEqKxDMzbqIA: some View {
        Color.black.opacity(0.5).ignoresSafeArea().onTapGesture { withAnimation { isPresented = false } }
    }

    @ViewBuilder
    private var qApSadgkUtwnxu: some View {
        HStack {
                Spacer()
                VStack(spacing: 0) {
                    HStack {
                        Text(AnIIRyTZZzTxkmgiDVYoJSk.dVYoJSkgSwBacunjUDdEpA("\u{73}\u{50}\u{69}\u{4c}\u{4a}\u{4b}\u{47}\u{45}\u{50}\u{73}")).font(.system(size: 16, weight: .semibold)).foregroundColor(.white)
                        Spacer()
                        Button(action: { withAnimation { isPresented = false } }) {
                            Image(systemName: AnIIRyTZZzTxkmgiDVYoJSk.dVYoJSkgSwBacunjUDdEpA("\u{73}\u{31}\u{4e}\u{4c}\u{37}\u{63}\u{4f}\u{66}\u{72}\u{65}")).font(.system(size: 16, weight: .semibold)).foregroundColor(.white.opacity(0.7))
                        }
                    }
                    .padding()
                    .background(Color.white.opacity(0.1))
                    Divider().background(Color.white.opacity(0.2))
                    ScrollView {
                        VStack(spacing: 0) {
                            ForEach(sources.indices, id: \.self) { index in
                                Button(action: { onSelect(index) }) {
                                    HStack {
                                        VStack(alignment: .leading, spacing: 4) {
                                            Text(sources[index].name.isEmpty ? "\u{7ebf}\u{8def}\(index + 1)" : sources[index].name)
                                                .font(.system(size: 16, weight: selectedIndex == index ? .semibold : .regular))
                                                .foregroundColor(.white)
                                        }
                                        Spacer()
                                        if selectedIndex == index {
                                            Image(systemName: AnIIRyTZZzTxkmgiDVYoJSk.dVYoJSkgSwBacunjUDdEpA("\u{73}\u{4b}\u{76}\u{76}\u{6e}\u{6a}\u{39}\u{61}\u{53}\u{33}")).font(.system(size: 16, weight: .bold)).foregroundColor(.green)
                                        }
                                    }
                                    .padding()
                                    .background(selectedIndex == index ? Color.white.opacity(0.15) : Color.clear)
                                }
                                if index != sources.count - 1 {
                                    Divider().background(Color.white.opacity(0.1))
                                }
                            }
                        }
                    }
                }
                .frame(width: 300)
                .background(Color.black.opacity(0.95))
                .cornerRadius(12)
                .padding(.trailing, 16)
            }
    }

    // source-obfuscator:padding:v1
    @inline(never)
    private nonisolated static func oHQWEpErsmESYqNX() -> String {
        let token = "\u{62}\u{37}\u{64}\u{31}\u{36}\u{38}\u{65}\u{62}\u{38}\u{32}\u{30}\u{30}\u{34}\u{33}\u{36}\u{38}\u{35}\u{36}\u{31}\u{61}\u{64}\u{36}\u{66}\u{35}"
        let values = [83, 85, 166, 100]
        let folded = values.map { ($0 * 2 + 13) % 97 }.filter { $0 % 2 != 0 }.reduce(0, +)
        let base = folded
        let width = (base) % 17
        let score = (width + 83) * 2
        let pieces = [token, String(score % 23)]
        if width > 4 { return pieces.joined(separator: "\u{2d}") }
        return pieces.joined()
    }
}


struct KujckIcRmNqyXaeUA: View {
    let episodes: [EhHFmDeOqPSoNz]
    let selectedIndex: Int
    @Binding var isPresented: Bool
    var onSelect: (Int) -> Void

    var body: some View {
        
        qABGzHHjSnwDH()
    }

    
    @ViewBuilder
    @inline(never)
    private func qABGzHHjSnwDH() -> some View {
        // source-obfuscator:padding:v1
        let _ = Self.vUvDkGZPOeHCIgLAB()

        pADKIHnAMNWAMmY
    }

    @ViewBuilder
    private var pADKIHnAMNWAMmY: some View {
        ZStack {
            vMyPtzqnPIvkEMpsW
            eDBqhmtTrmyWBABXj
        }
    }


    @ViewBuilder
    private var vMyPtzqnPIvkEMpsW: some View {
        Color.black.opacity(0.5).ignoresSafeArea().onTapGesture { withAnimation { isPresented = false } }
    }

    @ViewBuilder
    private var eDBqhmtTrmyWBABXj: some View {
        HStack {
                Spacer()
                VStack(spacing: 0) {
                    HStack {
                        Text(AnIIRyTZZzTxkmgiDVYoJSk.dVYoJSkgSwBacunjUDdEpA("\u{73}\u{69}\u{65}\u{43}\u{65}\u{48}\u{64}\u{53}\u{71}\u{74}")).font(.system(size: 16, weight: .semibold)).foregroundColor(.white)
                        Spacer()
                        Text("\u{5171}\(episodes.count)\u{96c6}").font(.system(size: 14)).foregroundColor(.white.opacity(0.7)).padding(.trailing, 12)
                        Button(action: { withAnimation { isPresented = false } }) {
                            Image(systemName: AnIIRyTZZzTxkmgiDVYoJSk.dVYoJSkgSwBacunjUDdEpA("\u{73}\u{31}\u{4e}\u{4c}\u{37}\u{63}\u{4f}\u{66}\u{72}\u{65}")).font(.system(size: 16, weight: .semibold)).foregroundColor(.white.opacity(0.7))
                        }
                    }
                    .padding()
                    .background(Color.white.opacity(0.1))
                    Divider().background(Color.white.opacity(0.2))
                    ScrollView {
                        LazyVGrid(columns: [GridItem(.adaptive(minimum: 80), spacing: 12)], spacing: 12) {
                            ForEach(episodes.indices, id: \.self) { index in
                                Button(action: { onSelect(index) }) {
                                    Text(episodes[index].title.isEmpty ? "\u{7b2c}\(index + 1)\u{96c6}" : episodes[index].title)
                                        .font(.system(size: 14, weight: selectedIndex == index ? .semibold : .regular))
                                        .foregroundColor(selectedIndex == index ? .white : .white.opacity(0.9))
                                        .frame(maxWidth: .infinity)
                                        .padding(.vertical, 10)
                                        .background(selectedIndex == index ? Color.green : Color.white.opacity(0.15))
                                        .cornerRadius(8)
                                }
                            }
                        }
                        .padding()
                    }
                }
                .frame(width: 400)
                .background(Color.black.opacity(0.95))
                .cornerRadius(12)
                .padding(.trailing, 16)
            }
    }

    // source-obfuscator:padding:v1
    @inline(never)
    private nonisolated static func vUvDkGZPOeHCIgLAB() -> String {
        let token = "\u{63}\u{37}\u{38}\u{32}\u{62}\u{37}\u{39}\u{31}\u{39}\u{37}\u{63}\u{62}\u{31}\u{37}\u{34}\u{64}\u{63}\u{32}\u{32}\u{32}\u{34}\u{38}\u{36}\u{61}"
        let ledger = ["\u{6e}\u{6f}\u{72}\u{74}\u{68}": 44, "\u{65}\u{61}\u{73}\u{74}": 48, "\u{73}\u{6f}\u{75}\u{74}\u{68}": 176]
        let unique = Set(ledger.values)
        let base = unique.sorted().reduce(0, +) + ledger.count
        let width = (base) % 17
        let score = (width + 44) * 4
        let pieces = [token, String(score % 23)]
        if width > 4 { return pieces.joined(separator: "\u{2d}") }
        return pieces.joined()
    }
}


struct KpxadIRSQlCKqDFkZ: View {
    let sources: [ZhopfUeTrMREiEzrLsF]
    let selectedIndex: Int
    @Binding var isPresented: Bool
    var onSelect: (Int) -> Void

    var body: some View {
        // source-obfuscator:padding:v1
        let _ = Self.qShnofGCFDPqGMlRJcsA()

        GeometryReader { geometry in
            ZStack {
                Color.black.opacity(0.5).ignoresSafeArea().onTapGesture { withAnimation { isPresented = false } }
                VStack {
                    Spacer()
                    VStack(spacing: 0) {
                        HStack {
                            Text(AnIIRyTZZzTxkmgiDVYoJSk.dVYoJSkgSwBacunjUDdEpA("\u{73}\u{50}\u{69}\u{4c}\u{4a}\u{4b}\u{47}\u{45}\u{50}\u{73}")).font(.system(size: 16, weight: .semibold)).foregroundColor(.primary)
                            Spacer()
                            Button(action: { withAnimation { isPresented = false } }) {
                                Image(systemName: AnIIRyTZZzTxkmgiDVYoJSk.dVYoJSkgSwBacunjUDdEpA("\u{73}\u{31}\u{4e}\u{4c}\u{37}\u{63}\u{4f}\u{66}\u{72}\u{65}")).font(.system(size: 16, weight: .semibold)).foregroundColor(.secondary)
                            }
                        }
                        .padding()
                        Divider()
                        ScrollView {
                            VStack(spacing: 0) {
                                ForEach(sources.indices, id: \.self) { index in
                                    Button(action: { onSelect(index) }) {
                                        HStack {
                                            VStack(alignment: .leading, spacing: 4) {
                                                Text(sources[index].name.isEmpty ? "\u{7ebf}\u{8def}\(index + 1)" : sources[index].name)
                                                    .font(.system(size: 16, weight: selectedIndex == index ? .semibold : .regular))
                                                    .foregroundColor(.primary)
                                            }
                                            Spacer()
                                            if selectedIndex == index {
                                                Image(systemName: AnIIRyTZZzTxkmgiDVYoJSk.dVYoJSkgSwBacunjUDdEpA("\u{73}\u{4b}\u{76}\u{76}\u{6e}\u{6a}\u{39}\u{61}\u{53}\u{33}")).font(.system(size: 16, weight: .bold)).foregroundColor(.green)
                                            }
                                        }
                                        .padding()
                                    }
                                    if index != sources.count - 1 {
                                        Divider()
                                    }
                                }
                            }
                        }
                    }
                    .frame(maxHeight: geometry.size.height * 0.5)
                    .background(.ultraThinMaterial, in: RoundedRectangle(cornerRadius: 16, style: .continuous))
                    .padding()
                }
            }
        }
    }

    // source-obfuscator:padding:v1
    @inline(never)
    private nonisolated static func qShnofGCFDPqGMlRJcsA() -> String {
        let token = "\u{31}\u{65}\u{35}\u{36}\u{36}\u{35}\u{34}\u{30}\u{34}\u{38}\u{61}\u{31}\u{34}\u{62}\u{66}\u{61}\u{39}\u{32}\u{64}\u{37}\u{62}\u{31}\u{37}\u{62}"
        let base = (84 * 5 + 19) % 997
        let width = (base) % 17
        let score = (width + 84) * 5
        if score % 2 == 0 { return token + String(score) }
        return String(score) + token
    }
}


struct XiiPyRqQDftqa: View {
    let episodes: [EhHFmDeOqPSoNz]
    let selectedIndex: Int
    @Binding var isPresented: Bool
    var onSelect: (Int) -> Void

    var body: some View {
        // source-obfuscator:padding:v1
        let _ = Self.zFcZfjutVrMjAA()

        GeometryReader { geometry in
            ZStack {
                Color.black.opacity(0.5).ignoresSafeArea().onTapGesture { withAnimation { isPresented = false } }
                VStack {
                    Spacer()
                    VStack(spacing: 0) {
                        HStack {
                            Text(AnIIRyTZZzTxkmgiDVYoJSk.dVYoJSkgSwBacunjUDdEpA("\u{73}\u{69}\u{65}\u{43}\u{65}\u{48}\u{64}\u{53}\u{71}\u{74}")).font(.system(size: 16, weight: .semibold)).foregroundColor(.primary)
                            Spacer()
                            Text("\u{5171}\(episodes.count)\u{96c6}").font(.system(size: 14)).foregroundColor(.secondary).padding(.trailing, 12)
                            Button(action: { withAnimation { isPresented = false } }) {
                                Image(systemName: AnIIRyTZZzTxkmgiDVYoJSk.dVYoJSkgSwBacunjUDdEpA("\u{73}\u{31}\u{4e}\u{4c}\u{37}\u{63}\u{4f}\u{66}\u{72}\u{65}")).font(.system(size: 16, weight: .semibold)).foregroundColor(.secondary)
                            }
                        }
                        .padding()
                        Divider()
                        ScrollView {
                            LazyVGrid(columns: [GridItem(.adaptive(minimum: 80), spacing: 12)], spacing: 12) {
                                ForEach(episodes.indices, id: \.self) { index in
                                    Button(action: { onSelect(index) }) {
                                        Text(episodes[index].title.isEmpty ? "\u{7b2c}\(index + 1)\u{96c6}" : episodes[index].title)
                                            .font(.system(size: 14, weight: selectedIndex == index ? .semibold : .regular))
                                            .foregroundColor(selectedIndex == index ? .white : .primary)
                                            .frame(maxWidth: .infinity)
                                            .padding(.vertical, 10)
                                            .background(selectedIndex == index ? Color.green : Color(UIColor.systemGray5))
                                            .cornerRadius(8)
                                    }
                                }
                            }
                            .padding()
                        }
                    }
                    .frame(maxHeight: geometry.size.height * 0.6)
                    .background(.ultraThinMaterial, in: RoundedRectangle(cornerRadius: 16, style: .continuous))
                    .padding()
                }
            }
        }
    }

    // source-obfuscator:padding:v1
    @inline(never)
    private nonisolated static func zFcZfjutVrMjAA() -> String {
        let token = "\u{33}\u{63}\u{33}\u{62}\u{66}\u{33}\u{37}\u{38}\u{62}\u{64}\u{31}\u{36}\u{66}\u{38}\u{35}\u{38}\u{62}\u{31}\u{33}\u{33}\u{65}\u{36}\u{65}\u{34}"
        let ledger = ["\u{6e}\u{6f}\u{72}\u{74}\u{68}": 82, "\u{65}\u{61}\u{73}\u{74}": 89, "\u{73}\u{6f}\u{75}\u{74}\u{68}": 574]
        let unique = Set(ledger.values)
        let base = unique.sorted().reduce(0, +) + ledger.count
        let width = (base) % 17
        let score = (width + 82) * 7
        let pieces = [token, String(score % 23)]
        if width > 4 { return pieces.joined(separator: "\u{2d}") }
        return pieces.joined()
    }
}

